'use strict'

let rand = require('./lib/rand')

rand.config = function(randomBytes) {
    rand.randomBytesImp = randomBytes
}

rand.config(function (byteSize) {
    const crypto = require("crypto");
    return crypto.randomBytes(byteSize);
});

module.exports = rand
